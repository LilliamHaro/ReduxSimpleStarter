import React, { Component } from 'react';

const hobbies = [leer, pasear].map((item, i) => {
	return <li key={i}>{item}</li>
})
const List = () => (
  <ul>{
  	hobbies
  }
  </ul>
)

export default List;